spring:
  application:
    name: NationalEngineeringEnterpriseService
  datasource:
    username: root
    password: althaf
    url: jdbc:mysql://localhost:3307/nationalengineering
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    hibernate:
      ddl-auto: create

